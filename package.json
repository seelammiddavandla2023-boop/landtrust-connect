{
  "name": "landtrust-connect",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "LandTrust Connect — an AI-based evidence-gated land ownership verification, secure owner interaction and autonomous transaction risk resolution system (Review-2 research prototype).",
  "scripts": {
    "setup": "npm run setup:backend && npm run setup:frontend",
    "setup:backend": "node scripts/setup-backend.mjs",
    "setup:frontend": "cd frontend && npm install",
    "seed": "node scripts/py.mjs --cwd backend -m app.seed.generate && node scripts/py.mjs --cwd backend -m app.seed.seed --reset",
    "eval": "node scripts/py.mjs --cwd backend -m app.eval.run_eval",
    "eval:ocr": "node scripts/py.mjs --cwd backend -m app.eval.run_eval --ocr",
    "test": "node scripts/py.mjs --cwd backend -m pytest -q",
    "dev": "concurrently -n api,web -c blue,green \"npm:dev:backend\" \"npm:dev:frontend\"",
    "dev:backend": "node scripts/py.mjs --cwd backend -m uvicorn app.main:app --reload --port 8000",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "concurrently -n api,web -c blue,green \"node scripts/py.mjs --cwd backend -m uvicorn app.main:app --port 8000\" \"cd frontend && npm run start\""
  },
  "devDependencies": {
    "concurrently": "9.0.1"
  }
}
