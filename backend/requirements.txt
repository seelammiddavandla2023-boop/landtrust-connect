# LandTrust Connect — backend dependencies.
# Everything here is open source and installs without an API key. The prototype runs
# fully offline in DEMO extraction mode; Tesseract is optional and only needed for the
# real-OCR path and the OCR arm of the evaluation harness.

fastapi==0.115.*
uvicorn[standard]==0.30.*
sqlalchemy==2.0.*
pydantic==2.*
pydantic-settings==2.*
python-multipart==0.0.*

# Document intelligence
pymupdf==1.24.*          # text-layer extraction, word geometry, PDF rendering
reportlab==4.*           # synthetic corpus generation
pytesseract==0.3.*       # OCR mode (requires the tesseract-ocr binary)
pillow==10.*             # image handling for the OCR path

# Testing
pytest==8.*
httpx==0.27.*            # FastAPI TestClient transport

# Optional — PostgreSQL instead of SQLite:
# psycopg[binary]==3.2.*
